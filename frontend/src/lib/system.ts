// Facts that don't vary by home. Per-home facts (array kWp, battery, inverter, tariff) live
// in data/homes.json and reach the pages through lib/home (useHome()).

/** 5-minute readings in a full day. */
export const READINGS_PER_DAY = 288;
